<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/bg_color"
    android:padding="32dp">

    <!-- Decorative Top Element -->
    <View
        android:id="@+id/decorative_top"
        android:layout_width="120dp"
        android:layout_height="120dp"
        android:background="@drawable/circle_accent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        android:layout_marginTop="-40dp"
        android:layout_marginEnd="-40dp" />

    <!-- Main Title -->
    <TextView
        android:id="@+id/main_title"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="@string/ob_3_main_title"
        android:textSize="28sp"
        android:textColor="@color/primary_color"
        android:textStyle="bold"
        android:letterSpacing="0.02"
        android:fontFamily="sans-serif-medium"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toTopOf="@+id/image"
        android:layout_marginTop="40dp" />

    <!-- Hero Image -->
    <ImageView
        android:id="@+id/image"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:src="@drawable/ob_3"
        android:contentDescription="Health tracking illustration"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/main_title"
        app:layout_constraintBottom_toTopOf="@+id/sub_title"
        app:layout_constraintWidth_percent="0.8"
        app:layout_constraintHeight_percent="0.4"
        app:layout_constraintVertical_chainStyle="packed"
        android:layout_marginTop="32dp"
        android:layout_marginBottom="32dp" />

    <!-- Subtitle -->
    <TextView
        android:id="@+id/sub_title"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="@string/ob_3_sub_title"
        android:textSize="18sp"
        android:textColor="@color/sub_title_text_color"
        android:lineSpacingExtra="4sp"
        android:textAlignment="center"
        android:fontFamily="sans-serif"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/image"
        app:layout_constraintBottom_toTopOf="@+id/decorative_bottom"
        android:layout_marginTop="16dp" />

    <!-- Decorative Bottom Element -->
    <View
        android:id="@+id/decorative_bottom"
        android:layout_width="60dp"
        android:layout_height="60dp"
        android:background="@drawable/circle_secondary"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        android:layout_marginStart="-20dp"
        android:layout_marginBottom="-20dp" />

</androidx.constraintlayout.widget.ConstraintLayout>